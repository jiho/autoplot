#'
#' @param data the original dataset, use to compute the contributions of rows and columns to the principal components. When \code{NULL}, the default, data will be extracted from the CA object when it contains it
#'
#' @param PC the principal components to extract; two is usual for plotting
#'
